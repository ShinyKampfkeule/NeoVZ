<h1>Mitarbeiterprofil</h1>

<form method="post" action="/edit/save">
    {{#each data}}
        <label>{{this.label}}</label>
        <input type="text" name="{{this.label}}" value="{{this.value}}">
    {{/each}}
    <button type="submit">Save</button>
</form>